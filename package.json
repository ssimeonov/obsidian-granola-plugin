{
	"name": "obsidian-granola-simple-plugin",
	"version": "1.0.0",
	"description": "Sync your Granola meeting notes to Obsidian",
	"main": "main.js",
	"scripts": {
		"dev": "node esbuild.config.mjs",
		"lint": "eslint .",
		"typecheck": "tsc --noEmit",
		"build": "node esbuild.config.mjs production",
		"package": "npm run build && mkdir -p release/granola-simple-sync && cp main.js manifest.json versions.json release/granola-simple-sync/ && cd release && zip -r granola-simple-sync.zip granola-simple-sync",
		"deploy-local": "source .env && npm run package && rm -rf \"$OBSIDIAN_PLUGINS/granola-simple-sync\" && mv release/granola-simple-sync \"$OBSIDIAN_PLUGINS/\"",
		"version": "node version-bump.mjs && git add manifest.json versions.json"
	},
	"keywords": [],
	"author": "Phil Freo",
	"license": "MIT",
	"devDependencies": {
		"@eslint/js": "^9.30.0",
		"@types/node": "^22.0.0",
		"esbuild": "^0.24.0",
		"eslint-plugin-obsidianmd": "^0.1.9",
		"globals": "^14.0.0",
		"jiti": "^2.6.0",
		"obsidian": "latest",
		"typescript": "^5.6.0",
		"typescript-eslint": "^8.35.0"
	},
	"dependencies": {
		"@tiptap/pm": "^3.0.0",
		"@tiptap/starter-kit": "^3.0.0",
		"@tiptap/static-renderer": "^3.0.0"
	}
}